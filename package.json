{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm install",
    "build2": "cd client && npm install vite --no-save",
    "install-client": "cd client && npm run build",
    "heroku-postbuild": "cd client && npm install --legacy-peer-deps && npm install vite --legacy-peer-deps && npm run build && cd .. && cp -R client/dist public",
    "start": "nodemon index",
    "client": "cd client && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cloudinary": "^1.37.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.3.0",
    "nodemon": "^2.0.22",
    "openai": "^3.3.0"
  }
}